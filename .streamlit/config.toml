[global]

# (Removed obsolete option `showSidebarNavigation` because newer Streamlit versions
# no longer support it. Keeping other global settings empty.)

[server]

# List of folders that should not be watched for changes. This impacts both "Run on Save" and @st.cache.
folderWatchBlacklist = ['']

[theme]

# The preset Streamlit theme that your custom theme inherits from.
# One of "light" or "dark".
base = "dark"

# Primary accent color for interactive elements.
primaryColor = "#1f77b4"

# Background color for the main content area (dark)
backgroundColor = "#0f1115"

# Background color for sidebar and most interactive widgets (dark)
secondaryBackgroundColor = "#0b0d10"

# Main text color
textColor = "#F0F2F5"

# Optional: adjusted primary font (keep default if you prefer)
font = "sans serif"